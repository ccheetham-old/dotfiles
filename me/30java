[[ -d $ME_ARCH_OPT/jdk ]] && JAVA_HOME=$ME_ARCH_OPT/jdk
if [[ -n $JAVA_HOME ]]; then
  shlog using java at $JAVA_HOME
  export JAVA_HOME
  is_in_path $JAVA_HOME/bin || prepend_path $JAVA_HOME/bin
  alias j='java'
fi

# vim: filetype=sh
