AC_INIT([myenv], [0.0.1])

AC_MSG_NOTICE([version... $VERSION])

AM_INIT_AUTOMAKE([tar-pax])

AC_OUTPUT([
Makefile
])

cat <<EOM
Configuration:
==============================================================================

    VERSION     = ${VERSION}

    prefix      = ${prefix}

==============================================================================
EOM
