if [[ -d $ME_ARCH_OPT/jdk ]]; then
  JAVA_HOME=$ME_ARCH_OPT/jdk
fi
if [[ -n $JAVA_HOME ]]; then
  shlog using java at $JAVA_HOME
  export JAVA_HOME
  PATH=$JAVA_HOME/bin:$PATH
  alias j='java'
fi

# vim: filetype=sh
